name: Renovate check

on:
  pull_request:
    paths:
      - renovate.json

permissions:
  contents: read
  pull-requests: read

jobs:
  renovate-check:
    uses: canonical/sdkcraft-actions/.github/workflows/renovate-check.yml@main
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
